# Ownership Matrix: <work-package-id>

| Task | Role | Files/interfaces/data objects | Write or read | Parallel conflict checked? | Evidence required |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

Record a conflict before assigning parallel work. Shared migrations, contracts, fixtures, generated output, and environment state are conflicts unless the approved contract explicitly resolves them.
